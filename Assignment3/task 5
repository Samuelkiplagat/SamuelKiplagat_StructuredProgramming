#include <stdio.h>

int main(){
int a=3,b=5;

void swapnumbers(int *x,int *y){

  int swap=*x;
   *x=*y;
   *y=swap;

}
printf("The values of a and b before calling the function are a=%d,b=%d \n",a,b);

swapnumbers(&a,&b);

printf("The new values after swapping are a=%d,b=%d \n",a,b);

return 0;}
